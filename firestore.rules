rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function isAuthenticatedAdmin() {
      return request.auth != null;
    }

    match /firmRegistry/{firmId} {
      allow read: if true;
      allow create, update, delete: if isAuthenticatedAdmin();
    }
  }
}
